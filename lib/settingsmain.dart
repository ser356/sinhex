import "package:flutter/material.dart";
import "package:sinhex/hex.dart";

class SettingsMain extends StatelessWidget {
  @override
  const SettingsMain({Key? key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: '222222'.toColor(),
      ),
    );
  }
}
